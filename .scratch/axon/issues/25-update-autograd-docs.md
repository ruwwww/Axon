# 25 — Update Autograd Documentation for Polymorphic Node Model

## What to build

Inspect `README.md`, `SPEC.md`, and `CONTEXT.md` for outdated references to `GraphNode` and `OpType`, and replace all mentions with the polymorphic `Node` base class and Node DAG autograd model.

## Blocked by

None — follows Ticket #21 (Polymorphic Node DAG Autograd Engine).

## Acceptance criteria

- [x] Inspect README.md, SPEC.md, and CONTEXT.md for outdated references to GraphNode and OpType
- [x] Replace all mentions of GraphNode and OpType with the polymorphic Node base class and Node DAG autograd model
- [x] Update ticket 25 status to completed and commit changes

## Status

completed
